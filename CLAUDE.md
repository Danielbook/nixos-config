# Claude Code Instructions

## Build Commands
- **Always use `make nixos-rebuild` instead of `sudo nixos-rebuild switch`**
- **Always use `make home-manager-switch` instead of `home-manager switch`**

These make targets provide consistent build behavior and proper error handling.