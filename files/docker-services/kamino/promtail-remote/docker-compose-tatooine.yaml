---
# Deploy this on Tatooine (10.10.40.12)
# Instructions:
# 1. Copy this file and promtail-config-tatooine.yaml to Tatooine machine
# 2. Run: docker compose -f docker-compose-tatooine.yaml up -d

services:
  promtail:
    container_name: promtail
    image: grafana/promtail:2.9.4
    volumes:
      - /var/log:/var/log:ro
      - /var/lib/docker/containers:/var/lib/docker/containers:ro
      - ./promtail-config-tatooine.yaml:/etc/promtail/promtail-config.yaml:ro
    restart: unless-stopped
    command: -config.file=/etc/promtail/promtail-config.yaml
    network_mode: bridge