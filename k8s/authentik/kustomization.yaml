# authentik env Secret (DB creds, AUTHENTIK_SECRET_KEY, email creds), decrypted
# in-cluster by ksops (same pipeline as k8s/immich, k8s/arr, k8s/mealie).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generators:
  - ./secret-generator.yaml
