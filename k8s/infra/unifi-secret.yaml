# unifi env Secret, decrypted in-cluster by ksops. Same pipeline as
# k8s/infra/authentik-secret.yaml.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: unifi-secret
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://github.com/Danielbook/nixos-config.git
    targetRevision: feat/cluster
    path: k8s/unifi
  destination:
    server: https://kubernetes.default.svc
    namespace: unifi
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
