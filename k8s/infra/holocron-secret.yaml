# holocron STT token + registry-pull secrets, decrypted in-cluster by ksops
# (same pipeline as fartlek-secret.yaml). See k8s/holocron/holocron-secrets.enc.yaml.
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: holocron-secret
  namespace: argocd
spec:
  project: default
  source:
    repoURL: https://forgejo.local.bookorjeman.com/danielbook/nixos-config.git
    targetRevision: main
    path: k8s/holocron
  destination:
    server: https://kubernetes.default.svc
    namespace: holocron
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
