# open-webui authentik OAuth client credentials, decrypted in-cluster by
# ksops (same pipeline as k8s/immich, k8s/arr).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
generators:
  - ./secret-generator.yaml
