# seerr (ghcr.io/seerr-team/seerr — not jellyseerr) — no GPU, no NFS media
# mount (talks to jellyfin/*arr over HTTP).
# Config (SQLite db, settings) is the iscsi PVC below. NOT migrated from the
# old tatooine container — unlike jellyfin/immich, no migrate pod was ever
# run for seerr before the D1 wipe, so this PVC started empty (D3, 2026-07-04).
apiVersion: v1
kind: Namespace
metadata:
  name: seerr
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: seerr-config
  namespace: seerr
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: iscsi
  resources:
    requests:
      storage: 2Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: seerr
  namespace: seerr
  labels:
    app: seerr
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: seerr
  template:
    metadata:
      labels:
        app: seerr
    spec:
      securityContext:
        fsGroup: 1000 # image runs as uid/gid 1000(node); iscsi PVC mounts root-owned otherwise
      containers:
        - name: seerr
          image: ghcr.io/seerr-team/seerr:v3.3.0 # not jellyseerr
          ports:
            - containerPort: 5055
          volumeMounts:
            - name: config
              mountPath: /app/config
      volumes:
        - name: config
          persistentVolumeClaim:
            claimName: seerr-config
---
apiVersion: v1
kind: Service
metadata:
  name: seerr
  namespace: seerr
spec:
  selector:
    app: seerr
  ports:
    - port: 80
      targetPort: 5055
